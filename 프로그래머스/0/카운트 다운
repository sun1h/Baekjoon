import java.util.*;

class Solution {
    public ArrayList solution(int start, int end_num) {
        ArrayList answer = new ArrayList<>();
        for(int i=start; i>end_num-1; i--){
            answer.add(i);
        }
        return answer;
    }
}
