class Solution {
    public int solution(String message) {
        int answer = message.length()*2;
        // for(int i=1; i<= message.length(); i++){
        //     answer+=2;
        // }
        return answer;
    }
}